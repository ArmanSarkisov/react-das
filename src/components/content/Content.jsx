import React  from 'react';

const Content = (props) => {
    // const [count, setCount] = useState(0);
    return (
        <>
            {props.children}
        </>
    )
}


export default Content;